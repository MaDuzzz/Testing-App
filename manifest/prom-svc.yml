apiVersion: v1
kind: Service
metadata:
  name: prom-svc
  namespace: lens-metrics
spec:
  type: NodePort
  selector:
    name: prometheus
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
      nodePort: 31050

